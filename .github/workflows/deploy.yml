# This workflow is no longer used. The site is now a single self-contained
# index.html served directly from the branch (Settings → Pages → Deploy from a
# branch → main → / root), so no build/deploy automation is needed.
# Kept as manual-only so it never runs automatically.
name: (unused) manual build
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Site is served as a static single-file index.html from the branch root."
